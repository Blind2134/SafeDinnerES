<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Color de relleno -->
    <solid android:color="@color/sky_blue_main" />

    <!-- Borde opcional -->
    <stroke
        android:width="1dp"
        android:color="@color/sky_blue_dark" />

    <!-- Tamaño opcional (si no se usa wrap_content) -->
    <size
        android:width="48dp"
        android:height="48dp" />

</shape>
